{
  "name": "badminton-tournament-manager",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm run dev:api\" \"pnpm run dev:web\" \"pnpm run dev:court-manager\"",
    "dev:api": "cd apps/api && pnpm dev",
    "dev:web": "cd apps/web && pnpm dev",
    "dev:court-manager": "cd apps/court-manager && pnpm dev",
    "install:all": "pnpm install && cd apps/api && pnpm install && cd ../web && pnpm install && cd ../court-manager && pnpm install",
    "build:all": "cd apps/api && pnpm build && cd ../web && pnpm build && cd ../court-manager && pnpm build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.17.1",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
