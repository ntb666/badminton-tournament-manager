# 手动分配赛程功能说明

## 功能概述

新增了手动分配赛程对阵信息的功能，允许管理员手动决定第一轮和第二轮的队伍分配，而不是通过自动随机方式。

## 主要特点

1. **显示赛程树结构**：根据队伍总数自动计算并显示赛程框架
2. **手动选择队伍**：为每个对阵位置手动选择参赛队伍
3. **轮空支持**：可以将某些位置留空表示轮空
4. **第二轮预分配**：支持将轮空队伍直接分配到第二轮位置

## 使用步骤

### 1. 进入手动分配页面

- 打开管理员控制台
- 点击 "手动分配赛程" 标签页
- 选择比赛类型（男双、女双、混双、男单、女单）

### 2. 查看赛程结构

系统会自动计算并显示：
- 总队伍数
- 赛程扩展大小（M值，2的幂次）
- 第一轮对阵场数
- 第二轮对阵场数

**示例**：
- 如果有10支队伍，系统会扩展到16个位置
- 第一轮：8场对阵
- 第二轮：4场对阵

### 3. 分配第一轮对阵

对于每场比赛，需要为位置A和位置B选择队伍：

- **选择队伍**：从下拉列表中选择队伍
- **设置轮空**：留空表示该位置轮空
- **查看已选**：选择后会显示确认信息

**注意**：
- 每支队伍只能分配一次
- 已分配的队伍在其他位置的下拉列表中会被禁用
- 允许部分位置轮空（null vs null 会被视为轮空对阵）

### 4. 分配第二轮对阵（可选）

第二轮的位置主要用于：
- 直接分配轮空队伍
- 预设某些已知的第二轮对阵

**留空表示**：该位置等待第一轮晋级者

### 5. 提交分配

- 点击 "确认提交" 按钮
- 系统会验证分配是否有效（无重复队伍）
- 成功后自动生成完整的赛程树

## 前端文件

### 新增组件

**ManualBracketAssignment.tsx**
```typescript
位置: apps/web/src/components/ManualBracketAssignment.tsx
功能: 手动分配赛程的主界面
```

**ManualBracketAssignment.module.css**
```css
位置: apps/web/src/components/ManualBracketAssignment.module.css
功能: 手动分配组件的样式
```

### 修改文件

**AdminPanel.tsx**
- 新增 "手动分配赛程" 标签页
- 集成 ManualBracketAssignment 组件
- 将原 "生成赛程" 改为 "自动生成赛程"

## 后端API

### 新增端点

**POST /api/schedule/generate-bracket-manual**

请求体：
```json
{
  "matchType": "MEN_DOUBLE",
  "manualAssignment": true,
  "round1": [
    {
      "matchIndex": 0,
      "teamAId": 1,
      "teamBId": 2
    },
    {
      "matchIndex": 1,
      "teamAId": null,  // 轮空
      "teamBId": 3
    }
  ],
  "round2": [
    {
      "matchIndex": 0,
      "teamAId": 4,      // 直接进入第二轮的轮空队伍
      "teamBId": null    // 等待第一轮晋级
    }
  ]
}
```

响应：
```json
{
  "success": true,
  "message": "成功手动生成男子双打赛程",
  "tournament": { ... },
  "rounds": 4,
  "matches": 15
}
```

## 赛程生成逻辑

### 算法说明

1. **计算赛程参数**
   - P = 实际队伍数
   - M = 2^⌈log₂(P)⌉（向上取整到2的幂次）
   - 第一轮位置数 = M / 2
   - 第二轮位置数 = M / 4

2. **创建比赛记录**
   - 第一轮：根据手动分配创建比赛
   - 第二轮：根据手动分配创建比赛（可部分待定）
   - 后续轮次：创建空框架，状态为 'pending'

3. **设置比赛状态**
   - `scheduled`: 双方队伍都已确定
   - `pending`: 至少有一方待定（等待晋级或轮空）
   - `bye`: 双方都为空（轮空对阵）

4. **建立父子关系**
   - 每两场比赛的胜者晋级到上一级比赛
   - 标准二叉树结构

## 使用场景

### 适合使用手动分配的情况

1. **特殊赛制要求**：某些队伍需要避免在特定轮次相遇
2. **VIP队伍安排**：重要队伍需要特定的比赛位置
3. **场地协调**：根据场地可用性安排特定队伍
4. **电视转播**：热门对阵需要安排在特定位置

### 适合使用自动分配的情况

1. **标准淘汰赛**：遵循常规种子分配规则
2. **快速组织**：队伍数量多，无特殊要求
3. **公平随机**：需要完全随机的对阵安排

## 验证规则

系统会验证以下内容：

1. ✅ 每支队伍最多分配一次
2. ✅ 队伍ID必须在可用队伍列表中
3. ✅ 第一轮和第二轮的分配互不冲突
4. ✅ 轮空队伍的处理正确

## 错误提示

可能出现的错误及解决方法：

| 错误信息 | 原因 | 解决方法 |
|---------|------|---------|
| "队伍重复分配" | 同一队伍被分配到多个位置 | 检查并移除重复分配 |
| "队伍数量不足" | 可用队伍少于2支 | 先添加队伍 |
| "手动生成赛程失败" | 服务器错误 | 检查后端日志 |

## 与自动生成的区别

| 特性 | 手动分配 | 自动生成 |
|-----|---------|---------|
| 控制程度 | 完全手动 | 随机+种子 |
| 适用场景 | 特殊要求 | 标准赛制 |
| 设置复杂度 | 较高 | 较低 |
| 耗时 | 较长 | 较短 |
| 灵活性 | 最高 | 较低 |

## 技术实现要点

### 前端

1. **状态管理**：使用对象存储所有位置的分配（键为 `R{round}-M{match}-{position}`）
2. **验证逻辑**：实时检查队伍重复
3. **UI交互**：下拉列表自动禁用已选队伍

### 后端

1. **数据结构**：接收第一轮和第二轮的分配数组
2. **比赛创建**：根据分配数据创建Match记录
3. **关系建立**：自动建立比赛的父子关系
4. **状态设置**：根据队伍情况设置正确的比赛状态

## 未来扩展

可能的功能增强：

1. 📊 **可视化预览**：在提交前显示完整赛程树预览
2. 💾 **保存草稿**：支持保存未完成的分配
3. 📋 **模板导入**：从文件导入预设的分配方案
4. 🔄 **批量操作**：支持批量分配或清空
5. 📱 **移动适配**：优化移动设备上的操作体验

## 总结

手动分配赛程功能为管理员提供了更高的灵活性，适合有特殊要求的比赛场景。通过直观的界面和完善的验证机制，确保分配的正确性和有效性。
